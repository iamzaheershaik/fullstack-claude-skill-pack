{
  "name": "bakeroute",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=packages/api",
    "dev:web": "npm run dev --workspace=packages/web",
    "dev:vendor": "npm run dev --workspace=packages/vendor",
    "dev:agent": "npm run dev --workspace=packages/agent"
  }
}
